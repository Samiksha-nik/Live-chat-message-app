# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Convex
NEXT_PUBLIC_CONVEX_URL=

# IMPORTANT: In .env.local, put each value on one line with NO trailing space/newline.
# Bad: NEXT_PUBLIC_CONVEX_URL="https://foo.convex.cloud"\n
# Good: NEXT_PUBLIC_CONVEX_URL=https://foo.convex.cloud

# Convex + Clerk auth:
# 1. Clerk: JWT template named "convex" — create via "Add template" → choose "Convex" (not Empty).
# 2. Convex Dashboard: Settings → Environment Variables → CLERK_JWT_ISSUER_DOMAIN = Issuer URL from Clerk (no trailing slash).
#    Example: https://pleased-alien-49.clerk.accounts.dev

# Optional: Clerk URLs (app already uses /sign-in, /sign-up, and redirects to /chat)
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
# NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat
